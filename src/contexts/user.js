/**
* A module to create a context related to user's status.
* @module contexts/user
* @author Jack
*/

import React from 'react';

const UserContext = React.createContext();

export default UserContext;